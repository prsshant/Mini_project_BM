# Mini_project_BM

Employee Registration System built with Node.js, Express, Mongoose (MongoDB), and bcrypt for password hashing.

## Features
- Employee registration endpoint (`POST /register`)
- Password hashing using `bcrypt`
- MongoDB data persistence with Mongoose
- Input validation and duplicate email check
